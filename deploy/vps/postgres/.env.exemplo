# O banco. Uma variavel so.
#
# O `instalar.sh` cria o .env de verdade e gera a senha. Este arquivo existe
# para dizer o que vai la, e vai versionado justamente por nao ter valor nenhum.
#
# A MESMA senha precisa estar em ../service/.env, como AM_DB_SENHA.
POSTGRES_PASSWORD=
